# NocoBase Multi-space

Portal runtime and administration for `@nocobase/plugin-multi-space`. The provider hydrates `spaces:my`, persists active/viewable spaces, contributes `x-spaces` and `x-spaces-view` to SDK and direct-upload/export request headers, and exposes a user-menu switcher. The manager covers space CRUD and membership. See `/dev/multi-space`.
