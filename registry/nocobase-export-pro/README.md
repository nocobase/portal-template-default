# NocoBase Export Pro

Adds `auto`, `sync`, and `async` processing to the base Portal export action and exports configured attachment fields as ZIP. Requires action-export, action-export-pro, and async-task-manager on the server. Its development scenario is added to the shared `/dev/export` entry.
