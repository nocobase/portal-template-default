# NocoBase LDAP authentication

Adds the LDAP account/password form to the Starter's dynamic sign-in page and
exports `useLdapSignIn` for application-owned login UI.

Installing the Registry also adds an LDAP component Demo under Authentication.
