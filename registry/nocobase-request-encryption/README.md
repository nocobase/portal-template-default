# NocoBase Request Encryption

Transparent Portal SDK query encoding compatible with `@nocobase/plugin-request-encryption`. Despite the upstream name, the protocol is Base64-encoded JSON rather than cryptographic encryption: request bodies and secrets receive no added confidentiality. Installing the Registry item enables the query transformer globally. See `/dev/request-encryption`.
