import assert from "node:assert/strict"; import fs from "node:fs"; const encoding = fs.readFileSync(new URL("../request-encoding.ts", import.meta.url), "utf8"); const provider = fs.readFileSync(new URL("../provider.tsx", import.meta.url), "utf8"); assert.match(encoding, /TextEncoder/); assert.match(encoding, /__encoded__/); assert.match(encoding, /JSON\.stringify/); assert.match(provider, /addQueryTransformer/); console.log("request encryption regression checks passed");
