# NocoBase Departments

Portal organization manager for `@nocobase/plugin-departments`. The UI covers hierarchy CRUD, member add/remove, and owner flags. Public adapters additionally cover user department/main/owner synchronization and role-department assignment. See `/dev/departments`.
