# NocoBase Export

Portal-native XLSX export for `@nocobase/plugin-action-export`. It supports application-owned columns, current filters, selected-record filters, relation paths, enum labels, data sources, and browser download. The server remains responsible for ACL and spreadsheet generation.

The development showcase is available at `/dev/export`.
